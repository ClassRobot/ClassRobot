# passive 被动应用插件

`passive` 放系统自动触发、但仍然属于用户业务的应用插件。

它和 `library` 的区别是：`passive` 的结果通常直接面向用户，`library` 的能力通常给其他插件调用。

## 放什么

- 自动欢迎、自动提醒、自动巡检、自动通知分发等业务。
- 基于事件、定时器或启动生命周期触发的用户可感知功能。

## 不放什么

- 单纯提供注入、采集、解析、发送底层能力的模块。
- 用户主动命令入口。

## 例子

```text
passive/
└── auto_notice/
```
